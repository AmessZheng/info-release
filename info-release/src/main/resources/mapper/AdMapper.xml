<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zhuxn.inforelease.orm.mapper.AdMapper">
    <resultMap id="AdResultMap" type="Ad">
        <id column="id" property="id" />
        <result column="create_time" property="createTime" />
        <result column="ad_type" property="adType" />
        <result column="content" property="content" />
        <result column="img_url" property="imgUrl" />
        <result column="ad_level" property="adLevel" />
        <result column="is_deleted" property="isDeleted" />
    </resultMap>

    <insert id="insertAd" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_ad(ad_type,content,img_url,ad_level)
        VALUES (#{adType},#{content},#{imgUrl},#{adLevel})
    </insert>
    
    <update id="updateAd">
        UPDATE t_ad
        SET ad_type = #{adType},
            content = #{content},
            img_url = #{imgUrl},
            ad_level = #{adLevel}
        WHERE id = #{id}
    </update>

    <update id="deleteAd">
        UPDATE t_ad
        SET is_deleted = TRUE
        WHERE id = #{id}
    </update>

    <select id="selectPageList" resultMap="AdResultMap">
        SELECT id,ad_type,content,img_url,ad_level
        FROM t_ad
        WHERE is_deleted = FALSE
        <if test="adType != null and adType != ''">
            AND ad_type = #{adType}
        </if>
        ORDER BY create_time DESC, ad_level DESC
        LIMIT #{start},#{pageSize}
    </select>

    <select id="selectCount" resultType="int">
        SELECT count(1)
        FROM t_ad
        WHERE is_deleted = FALSE
        <if test="adType != null and adType != ''">
            AND ad_type = #{adType}
        </if>
    </select>

    <select id="selectForView" resultMap="AdResultMap">
        SELECT id,ad_type,content,img_url,ad_level
        FROM t_ad
        WHERE is_deleted = FALSE
        <if test="adType != null and adType != ''">
            AND ad_type = #{adType}
        </if>
        ORDER BY ad_level DESC
    </select>
</mapper>